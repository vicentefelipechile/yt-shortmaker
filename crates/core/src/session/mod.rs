// =================================================================================================
// session — SQLite session DB stub
// =================================================================================================

use anyhow::Result;

pub fn init_db(_path: &std::path::Path) -> Result<()> {
    Ok(())
}
