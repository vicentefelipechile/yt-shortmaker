// =================================================================================================
// plano::preview — Preview stub
// =================================================================================================

pub fn generate_preview_bytes(_plano: &super::schema::Plano) -> Vec<u8> {
    Vec::new()
}
